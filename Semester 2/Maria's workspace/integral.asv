clear all, close all
clc

load('filename with space.mat')

% Define the parameters - placeholders atm
B = 1; 
O = 2; 
p = 3; 

% grid domain vals
z = -3:.1:3; 

% Define the function f(z) = p(exp(iBOz))
f = p * exp(1i * Beta_p * O * z);

I = trapz(f);
disp(I);

